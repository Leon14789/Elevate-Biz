In order to enjoy 100% of the project's capacity, you must run the following commands
npm i
composer i


HELP ME DEV SENIOR

when running the commands

php artisan breeze:install blade
 
php artisan migrate
npm install
npm run dev



Buscar como ocultar comentarios ao dar inspecionar no codigo

Dentro deste projeto existe um seeder para testes e criacao de uma jornada de trabalho que vem 
assosiada a um ID de um usuario o nome do seeder é CreateHoursWorkeedSeeder

com base no mesmo conceito temos o sedder para a criacao de usuarios chamado CreateUsersSeeder

Dentro do projeto é usado para carretgar imgs a funcao asset() que carrega a partir da pasta public

Antes de mostrar para o murillo lembrar de colocar todas as variaves com o padrao
de todas a primeira letra minuscula e as demais maisculas   

Pedir ajuda tmb para poder exibir uma mensagem para quando o usuario já tiver colocado os registros do dia 
mensagem tipo um alert na tela tipo Batimentos do dia já realizados 

Tema 
o tema neste projeto funciona assim 
temos 4 rotas cada uma com um nome porem todas chamam a mesma função que verifica o nome 
da rota e devolve um valor para $theme com base nisso pegamos o 
Session::put('NOVOVALORDATABELA', VALORADICIONADO); e adicionamos um campo chamado theme e preenchemos com 
o valor do if apos isso nas rotas CSS usamos o Session::get ou seja resgatamos esse valor da sessao e preenchemos ele 
na rota para definir o que sera chamado 